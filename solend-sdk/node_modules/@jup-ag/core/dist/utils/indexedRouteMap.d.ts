export interface IndexedRouteMap {
    mintKeys: string[];
    indexedRouteMap: {
        [key: number]: number[];
    };
}
/** An indexed route map which is light (less data to compress/decompress) and can quickly be inflated into the full route map  */
export declare function routeMapToIndexedRouteMap(routeMap: Map<string, string[]>): {
    mintKeys: string[];
    indexedRouteMap: Record<number, number[]>;
};
export declare function indexedRouteMapToRouteMap(indexedRouteMap: IndexedRouteMap): Map<string, string[]>;
